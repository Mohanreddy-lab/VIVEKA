# Local LLM (default — free, offline, private)
langchain>=0.3,<0.4
langchain-core>=0.3,<0.4
langchain-ollama>=0.2,<0.4
langchain-community>=0.3,<0.4

# Cloud LLM providers (both optional — only one is used at runtime)
langchain-google-genai>=2.0,<3.0
# langchain-huggingface omitted: pulls transformers<5 which conflicts with gradio 6 (huggingface-hub>=1.2)
# sentence-transformers omitted: same transitive conflict via transformers<5
# On HF Spaces, embeddings + LLM use huggingface_hub.InferenceClient (already a gradio dep)
# Locally, install sentence-transformers manually for offline embeddings

# Vector search
faiss-cpu>=1.7,<2.0

# Scoring
scikit-learn>=1.4,<2.0
numpy>=1.26,<3.0
pandas>=2.1,<3.0

# UI (gradio is injected by HF Spaces — do not pin here)
plotly>=5.20,<6.0

# Terminal output
rich>=13.0,<15.0

# Utilities
python-dotenv>=1.0,<2.0
tqdm>=4.66,<5.0
requests>=2.31,<3.0
