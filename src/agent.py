# agent.py — Pillar 4 orchestrator
# Decomposes the job description, runs the full 5-stage pipeline
# (jd_parser → recall → scoring → rerank → output), and returns
# the final shortlist with explanations.
